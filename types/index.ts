export interface Post {
  body: string
  id: number
  title: string
}

export interface ModelFunctionPackage {
  title: string
  description: string
  developers: string[]
  authors: string[]
  active: boolean
}
